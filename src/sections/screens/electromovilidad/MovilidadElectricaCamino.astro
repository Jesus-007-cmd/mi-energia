---

const movilidadList = [
  { 
    title:'Impulsar la eficiencia',
    description:'La transición a una flota de vehículos eléctricos optimiza el consumo de energía y reduce los costos operativos, tanto a corto como a largo plazo.'
  },
  {
    title:'Reducir costos',
    description:'Las empresas informan una disminución del 50% en los costos de mantenimiento al cambiar de vehículos tradicionales de gasolina a una flota eléctrica.'
  },
  {
    title:'Prepárate para el futuro',
    description:'Las empresas que adoptan la movilidad eléctrica se posicionan como líderes en sostenibilidad y responsabilidad social.'
  },
  {
    title:'Predicar con el ejemplo',
    description:'Demuestre su compromiso con la sostenibilidad y la reducción de emisiones nocivas al hacer la transición a una flota eléctrica.'
  }
];

const backgroundStyle = {
  backgroundImage: 'url(/images/screens/electromovilidad/img24.jpg)',
  backgroundSize: 'cover',
  backgroundPosition: 'center'
}

---
<div class="bg-gray-100 mx-4 md:mx-20 lg:mx-40 xl:mx-60">
  <!-- Primer grupo: 1 y 2 -->
  <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 items-start">
    {
      movilidadList.slice(0, 2).map((row, index) => 
        <div class="text-center border rounded-lg bg-white p-6 flex flex-col items-center shadow-lg hover:shadow-2xl transition-shadow duration-300 transform hover:scale-105"> 
          <div class="w-16 h-16 rounded-full border bg-gray-100 mb-6 flex items-center justify-center transition-all duration-300 transform hover:rotate-12 hover:scale-110">
            <span class="text-blue-700 font-semibold text-2xl">{index + 1}</span>
          </div>
          <h4 class="font-semibold text-base md:text-lg lg:text-xl text-gray-800 mb-2">{row.title}</h4>
          <p class="text-sm md:text-base lg:text-lg text-gray-600">{row.description}</p>
        </div>
      )
    }
  </div>

  <!-- Imagen de fondo centrada -->
  <div class="flex justify-center py-6">
    <div style={backgroundStyle} class="h-64 md:h-96 w-full md:w-1/2 mx-auto rounded-lg shadow-xl"></div>
  </div>

  <!-- Segundo grupo: 3 y 4 -->
  <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 items-start">
    {
      movilidadList.slice(2, 4).map((row, index) => 
        <div class="text-center border rounded-lg bg-white p-6 flex flex-col items-center shadow-lg hover:shadow-2xl transition-shadow duration-300 transform hover:scale-105"> 
          <div class="w-16 h-16 rounded-full border bg-gray-100 mb-6 flex items-center justify-center transition-all duration-300 transform hover:rotate-12 hover:scale-110">
            <span class="text-blue-700 font-semibold text-2xl">{index + 3}</span>
          </div>
          <h4 class="font-semibold text-base md:text-lg lg:text-xl text-gray-800 mb-2">{row.title}</h4>
          <p class="text-sm md:text-base lg:text-lg text-gray-600">{row.description}</p>
        </div>
      )
    }
  </div>

</div>

