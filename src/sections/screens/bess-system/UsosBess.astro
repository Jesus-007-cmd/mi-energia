---

const usoBessList = [
  { 
    title:'Reducción de los picos de demanda',
    description:'Nuestros sistemas permiten controlar y reducir los picos de demanda de energía para tener una curva de consumos más estable y evitar cargos adicionales por los picos en horas de arranque o de Alta producción.',
    img:'/images/screens/bess-system/img6.jpg'
  },
  {
    title:'Desplazamiento de carga bajo tarifas horarias',
    description:'El sistema de almacenamiento permite descargar y abastecer energia en los horarios en los que las tarifas de consumo son más caras',
    img:'/images/screens/bess-system/img7.jpg'
  },
  {
    title:'Garantiza continuidad operativa y calidad de la energía',
    description:'Los BESS funcionan como respaldo inmediato de la red para proveer energía en caso de que haya un corte o intermitencias.',
    img:'/images/screens/bess-system/img8.jpg'
  }
];

---

<section class="flex flex-col gap-6 p-6 md:px-20 lg:px-40 xl:px-60 bg-neutral-100">

  <h3 class="text-2xl font-semibold text-center text-gray-800 mb-6">Usos de los BESS</h3>

  <div class="flex flex-col gap-6">

    {usoBessList.map((row) => (
      <div class="flex flex-col lg:flex-row gap-6">

        <!-- Contenedor de texto con borde y sombra -->
        <div class="p-6 bg-white border-2 border-gray-300 rounded-lg shadow-md w-full hover:scale-105 transition-all duration-300 ease-in-out">
          <h3 class="font-semibold text-xl text-gray-800 mb-2">{row.title}</h3>
          <p class="text-base text-gray-600">{row.description}</p>
        </div>

        <!-- Imagen con efecto de agrandamiento y sombra -->
        <div class="flex justify-center">
          <img src={row.img} alt={row.title} class="w-full lg:w-96 rounded-md shadow-lg transition-transform duration-300 ease-in-out hover:scale-105"/>
        </div>

      </div>
    ))}

  </div>

</section>
